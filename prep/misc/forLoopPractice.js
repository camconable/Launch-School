let names = ['Chris', 'Kevin', 'Naveed', 'Pete', 'Victor', 'Willy'];
let upperNames = [];

// console.log(`names: ${names}`);

for (let i = 0; i < names.length; i += 1) {
  let upperCaseName = names[i].toUpperCase();
  upperNames.push(upperCaseName);
}

// console.log(`upperNames: ${upperNames}`);
